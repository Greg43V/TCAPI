[build]
  command = "npm install"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["package.json"]

[functions."tc-poll"]
  schedule = "* * * * *"
